{
  "info": {
    "_postman_id": "tournament-api-collection",
    "name": "🏆 API Torneos E-Sports - Casos de Prueba TDD/BDD (modificada)",
    "description": "Colección adaptada a las respuestas definidas en responses.js (success/data + error con code, message, details, timestamp).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Crear Torneo - Nombre inválido",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('Status 400', () => pm.response.to.have.status(400));",
              "const json = pm.response.json();",
              "pm.expect(json.success).to.be.false;",
              "pm.expect(json.error).to.be.an('object');",
              "pm.expect(json.error.message).to.include('nombre');",
              "pm.expect(json.error.code).to.be.oneOf(['BAD_REQUEST','VALIDATION_ERROR']);",
              "pm.expect(json.error.timestamp).to.be.a('string');"
            ],
            "type": "text/javascript"
          }
        }
      ]
    }
  ]
}